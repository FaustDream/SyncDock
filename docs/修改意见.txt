添加仓库窗口界面
1、添加仓库时太慢，改成增加动画进度效果或者加载效果或者一个直观的输出进度效果，导致用户以为页面卡住。
2、添加仓库时会不停弹出cmd的窗口，直到添加成功，要取消这个效果
3、添加仓库时要将对应仓库名称默认填充到显示名称
4、添加仓库时分组要是下拉框形式，值是已有的分组，如果不选直接输入分组，那便是新的分组
5、添加仓库窗口中添加仓库按钮和备注隔开一些，目前离得太近了
仓库界面
1、新建分组按钮都去掉，改成显示分组的一行导航最右侧有一个加号，点加号就是新增分组
2、刷新状态、同步全部和同步当前分组、同步仓库时都是弹出大量cmd窗口要去掉，取消这个效果
3、刷新状态、同步全部和同步当前分组、同步仓库时增加一个动画进度效果或者加载效果或者一个直观的输出进度效果
4、增加一个强制同步全部和强制同步当前分组、强制同步仓库按钮。用于以云端仓库为主，强制将云端覆盖本地仓库，避免同步仓库一直有被跳过
5、每个仓库的组去掉，直接显示分组名称
6、仓库对应所在路径不要在仓库页面上显示，只能通过点击仓库进入到仓库详情查看
7、清单下面的搜索框和分组框、状态款放在一行
8、清单下面的权限改成一个选择框的形式，选中就算全选，取消选中就算取消全选。同步按钮缩小到现在的1/8
9、日志默认显示全部的日志，然后才是可选仓库的日志
任务界面
1、当前任务运行中。但是任务结束不会变成已完成
2、已经有了已完成 x/x，成功 x，跳过 x (状态字体用颜色区分)，失败 x,就不要下面的成功 x，跳过 x，失败 x 按钮形式的了，导致显示效果重复了
3、进行同步全部的任务或者强制同步全部或者分组同步、强制分组同步都要跳到当前任务页面然后出现执行动画效果，完成一个显示已完成一个，进行下一个，并且显示对应的完成仓库日志和正在完成的任务仓库
4、运行摘要已迁移到任务概览标签。去掉
5、仓库结果和任务详情合并了，都是重复功能
6、任务详情标签页选项去掉，改成从历史任务点进去窗口查看
7、历史任务的日期组件显示的 yyyy/mm/日 改成正确的显示
设置界面
1、关于界面 增加检查更新设置，检查到更新，就打开对应GitHub的软件下载页面
2、同步模式增加几种（模式增加说明）可修改
3、配置目录、日志目录、默认扫描目录的提示和按钮放到对应的当前配置目录、当前日志目录 默认扫描目录 路径下面
4、关于界面不出现配置目录和日志目录显示
其他
1、界面很卡顿，打开时一直在加载，需要增加一个软件加载动画效果
2、软件使用效果不好，提升系统流畅效果。要从那几个方面提升，给出点建议
3、有没有相关的前端（React 18 + TypeScript+Components）ui组件
4、软件图标更换成 猫咪站在小船上在水上漂流的svg图，意象图就行
5、所有页面的仓库排序都是由问题的排前面，没问题的放后面
6、刷新状态要在不同步情况下刷新状态，目前会将一些已经同步的仓库还是跳过状态更新了，要重新进行检查。所有仓库都要进行检查一遍